import React from 'react';

import { Container } from './style';

const JustTitleNotices = (props) => {
    return(

        <Container>{props.title}</Container>
    );
}

export default JustTitleNotices;